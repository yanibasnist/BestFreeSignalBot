services:
  - type: web
    name: best-free-signal-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    port: $PORT  # استفاده از پورت از متغیر محیطی Render
