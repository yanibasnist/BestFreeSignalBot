services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"  # فرمانی که برای اجرای ربات استفاده می‌کنید
